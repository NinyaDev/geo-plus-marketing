version: '3.8'

services:
  # GPT Researcher — Python sidecar for deep research
  gpt-researcher:
    image: gptresearcher/gpt-researcher
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - TAVILY_API_KEY=${TAVILY_API_KEY}
    restart: unless-stopped
